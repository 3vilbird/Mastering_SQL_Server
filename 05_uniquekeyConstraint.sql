
-- unique key 

--- this defines the uiqueness like email,adhaar , Pancard all should be unique

-- we can handle the uniqueness with primary key but one tabel can have only single primary key
-- soo there comes the unique key

-- A table can have, only one primary key. If you want to enforce uniqueness on 2 or more columns, 
-- then we use unique key constraint.


-- general syntax to add identity 

-- To create the unique key using a query:
-- Alter Table Table_Name
-- Add Constraint Constraint_Name Unique(Column_Name)


